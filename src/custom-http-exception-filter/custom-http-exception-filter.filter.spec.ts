import { CustomHttpExceptionFilterFilter } from './custom-http-exception-filter.filter';

describe('CustomHttpExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new CustomHttpExceptionFilterFilter()).toBeDefined();
  });
});
